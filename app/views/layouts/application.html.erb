<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <title>PoP</title>
    <meta name="description" content="Pool of Printables is the world's first 3D designs crowdfunding platform. We connect 3D printer owners with common needs so they can find their validated 3D designers to get the job done."/>
    <%= csrf_meta_tags %>
    <%= action_cable_meta_tag %>
    <%= stylesheet_link_tag 'application', media: 'all' %>
    <%= favicon_link_tag asset_path('logo.png') %>
    <%#= stylesheet_pack_tag 'application', media: 'all' %> <!-- Uncomment if you import CSS in app/javascript/packs/application.js -->
  </head>
  <body>
    <div style="min-height: 100vh;">
    <% if controller_name == 'pages' && action_name == "home" %>
      <%= render "shared/transparent-navbar" %>
    <% else %>
      <%= render "shared/navbar" %>
    <% end %>
    <%= render 'shared/flashes' %>
    <%= yield %>
    </div>
    <%= render 'shared/footer' %>

    <%= javascript_include_tag 'application' %>
    <%= javascript_pack_tag 'application' %>
  </body>
</html>
